// Version simple multiplicacion arrays, CON ERRORES SINTACTICOS
int[3] a = {{1,2,3}; //Error sintactico (2, 12) elemento inesperado '{' 
int[3] b = {9,8,7};
int[3] c; 

int i = 0; 
for(i < 3){ //error sintactico fila 7 columna 10 elemento inesperado ')'
	c[i] = a[i]*b[i];
	i++; // O i +=1
}

// Programa de ejemplo otro.cc

//~ #include a=e3;
//~ #include a=e3;


enum yes = {uno,dos,tres,catorce};// prueba de enums

if(lol <  3) //prueba de ifs

else if() 
else if() ;
print(324 +2);


//~ // prueba de función, for y asignaciones
int factFor(int n){
    //int fact;
    int a *= 1;
    for(int n = 2; i <= n; ++i) // error for
        fact = fact * i;
    
    return return fact;
}

// prueba de while
int factWhile(int n) // error funcion
    int fact;
    fact *= 1;
    int i = 2;
    while(i <= n){
        fact = fact * i;
        i++;
    }
    return fact;
}

class hola{
    int a *= 34 * **123;
    bool b;
    int a *= 34 * **123;
    
    int hola2(,){
        print("hello");
        return 2;
    }
    int hola2(,){
        print("hello");
        return 2;
    }
    int adios()}
    int adios()
}


//~ // prueba de función sin argumentos, if y switch
void trying(la,lo,int n){ // error en funcion
    bool ok = true;
    int n = 4;
    
    if(ok)
    print("hola");
    else print("adios");
    
    switch(17){ // switch 1: no iden
        case (1 3+ 23) 
            {print("jaja");}
        case 2)
            { print("jeje");}
        case (3) 
            {print("jiji");}
        case (4)
            { print("jojo");}
        default *{print("juju");}
    }
    switch 32+3){
        case (en1) 
            {print("jaja");}
        case (en2)
            { print("jeje");}
        case (en3) 
            {print("jiji");}
        case (en4)
            { print("jojo");}
    }
    
    (enumerable){
        case (en1)
        {print("jaja");}
        case (en2)
        { print("jeje");}
        case (en3)
        {print("jiji");}
        case (en4)
        { print("jojo");}
    }
}

int main(){
    int n = 3;
    
    int a = factFor(n);
    int b = factWhile(n);
    trying();
    
    for(int n = 0; 3 *+ 9; ++i){ //For 1: mala E0
    }
    
    while(3 *+ 9){ //While 1: mala E0
        int a *= 19;
        int b = 4 ++ 5;
    }
    
    for(la; 3 *+ 9; ++i){ //For 2: 2 errores (debería tragarse todo y mostrar el primero) e instrucciones
        int a *= 19;
        int b = 4 ++ 5;
    }
    using ll = la *2;
    
    if a = 7) // if 1: mal condición
    int a = 4;
    
    if(a = 7) // if 2: mal instructs
    int 7 = mal; // da como mal el igual y no el 7?
    
    if(a = 7){ // if 3: múltiples instructs
        int a = 4;
        int b = 9;
       // a *= b;
        a *= b;
        a += b; 
        int c = 9;
         a += 3; 
    }
    
    
    
    return 0;
}

